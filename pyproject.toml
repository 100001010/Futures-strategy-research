[tool.poetry]
name = "futures-strategy-research"
version = "0.1.0"
description = ""
authors = ["eric"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.3.2"
matplotlib = "^3.10.5"
numpy = "^2.3.2"
statsmodels = "^0.14.5"
requests = "^2.32.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
